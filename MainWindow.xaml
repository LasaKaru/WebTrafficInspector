<Window x:Class="WebTrafficInspector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Traffic Inspector" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Icon="logo.ico">

    <Window.Resources>
        <Style TargetType="GridSplitter">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
        </Style>

        <Style TargetType="TextBox" x:Key="MonospaceTextBox">
            <Setter Property="FontFamily" Value="Consolas, Courier New"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="Background" Value="#F8F8F8"/>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Menu Bar -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_New Session" Click="NewSession_Click" InputGestureText="Ctrl+N"/>
                <MenuItem Header="_Open Session..." Click="OpenSession_Click" InputGestureText="Ctrl+O"/>
                <MenuItem Header="_Save Session" Click="SaveSession_Click" InputGestureText="Ctrl+S"/>
                <MenuItem Header="Save Session _As..." Click="SaveSessionAs_Click" InputGestureText="Ctrl+Shift+S"/>
                <Separator/>
                <MenuItem Header="Recent Sessions" Name="RecentSessionsMenu"/>
                <Separator/>
                <MenuItem Header="E_xit" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="_Session">
                <MenuItem Header="Session _Info" Click="SessionInfo_Click"/>
                <MenuItem Header="_Export Traffic..." Click="ExportTraffic_Click"/>
                <MenuItem Header="_Import Traffic..." Click="ImportTraffic_Click"/>
            </MenuItem>
        </Menu>

        <!-- Status Bar -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Name="StatusText" Text="Ready"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Name="SessionNameText" Text="Untitled Session" Margin="0,0,10,0"/>
                    <TextBlock Name="TrafficCountText" Text="Traffic: 0"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="1.5*"/>
            </Grid.RowDefinitions>

            <!-- URL Bar -->
            <Border Grid.Row="0" Background="#F0F0F0" Padding="10">
                <DockPanel>
                    <Button DockPanel.Dock="Right" Content="Go" Width="60" Margin="5,0,0,0" 
                            Click="NavigateButton_Click" Background="#4CAF50" Foreground="White" 
                            BorderThickness="0" Padding="10,5"/>
                    <Button DockPanel.Dock="Right" Content="New Window" Width="100" Margin="5,0,0,0"
                            Click="NewWindowButton_Click" Background="#2196F3" Foreground="White"
                            BorderThickness="0" Padding="10,5"/>
                    <TextBox Name="UrlTextBox" VerticalContentAlignment="Center" Padding="5"
                             KeyDown="UrlTextBox_KeyDown" Text="https://httpbin.org/get"/>
                </DockPanel>
            </Border>

            <!-- WebView2 Browser -->
            <Border Grid.Row="1" BorderBrush="#CCCCCC" BorderThickness="1">
                <wv2:WebView2 Name="WebViewControl"/>
            </Border>

            <!-- Horizontal Splitter -->
            <GridSplitter Grid.Row="2" Height="5" ResizeDirection="Rows"/>

            <!-- Traffic Log -->
            <Border Grid.Row="3" BorderBrush="#CCCCCC" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#E8E8E8" Padding="5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Captured Traffic" FontWeight="Bold" VerticalAlignment="Center"/>
                            <Button Content="Clear" Margin="10,0,0,0" Click="ClearButton_Click" 
                                    Background="#ff0000" Foreground="White" BorderThickness="0" Padding="8,3"/>
                            <TextBlock Name="FilterText" Text="" Margin="20,0,0,0" VerticalAlignment="Center" FontStyle="Italic"/>
                        </StackPanel>
                    </Border>

                    <DataGrid Grid.Row="1" Name="TrafficDataGrid" AutoGenerateColumns="False" 
                              SelectionMode="Single" SelectionChanged="TrafficDataGrid_SelectionChanged"
                              GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                              RowHeaderWidth="0" CanUserAddRows="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="50"/>
                            <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat=HH:mm:ss.fff}" Width="80"/>
                            <DataGridTextColumn Header="Method" Binding="{Binding Method}" Width="70"/>
                            <DataGridTextColumn Header="Host" Binding="{Binding Host}" Width="200"/>
                            <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="*"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="60"/>
                            <DataGridTextColumn Header="Length" Binding="{Binding Length}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Horizontal Splitter -->
            <GridSplitter Grid.Row="4" Height="5" ResizeDirection="Rows"/>

            <!-- Request/Response Pane -->
            <Border Grid.Row="5" BorderBrush="#CCCCCC" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Request Pane -->
                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="#E3F2FD" Padding="5">
                            <TextBlock Text="Request" FontWeight="Bold"/>
                        </Border>

                        <TextBox Grid.Row="1" Name="RequestTextBox" Style="{StaticResource MonospaceTextBox}"/>
                    </Grid>

                    <!-- Vertical Splitter -->
                    <GridSplitter Grid.Column="1" Width="5" ResizeDirection="Columns"/>

                    <!-- Response Pane -->
                    <Grid Grid.Column="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="#E8F5E8" Padding="5">
                            <TextBlock Text="Response" FontWeight="Bold"/>
                        </Border>

                        <TextBox Grid.Row="1" Name="ResponseTextBox" Style="{StaticResource MonospaceTextBox}"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </DockPanel>
</Window>
